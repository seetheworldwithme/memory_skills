{
  "mcpServers": {
    "memory-skills": {
      "type": "http",
      "url": "${MEMORY_SKILLS_MCP_URL:-http://127.0.0.1:8422/mcp}",
      "headers": {
        "Authorization": "Bearer ${MEMORY_SKILLS_ACCESS_KEY}"
      }
    }
  }
}
